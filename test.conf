server
{
    listen 80;
    server_name localhost;

    error_page /404.html;

    client_max_body_size 1M;

    root ../public;

    index index.html;

    location /
    {
        root /var/www;
        autoindex on;
        index index.html;
    }

    location /test.php
    {
        root /var/phpscripts;
        cgi_pass /usr/bin/php;
    }
}

server
{
    listen 443;
    server_name google.com;

    index index.html;

    root /var/www;
}